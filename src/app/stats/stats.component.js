import controller from './stats.component.controller';
import template from './stats.component.html';
import './stats.component.styl';

const StatsComponent = {
  template,
  controller,
  controllerAs: 'vm'
}

export default StatsComponent;
